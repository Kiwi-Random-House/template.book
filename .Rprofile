.First <- function(){
    is_integrating <- function() identical(Sys.getenv("CI"), "true")
    if(is_integrating()) return()
    message("To enable book live preview, go to: RStudio Addins -> BOOKDOWN -> Preview Book")
}
